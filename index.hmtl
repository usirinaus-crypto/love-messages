<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Я люблю тебя ❤️</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .container {
            background-color: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            padding: 40px;
            max-width: 800px;
            width: 100%;
            text-align: center;
        }
        
        h1 {
            color: #ff4757;
            margin-bottom: 30px;
            font-size: 2.5em;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
        }
        
        .instructions {
            color: #666;
            font-size: 1.2em;
            margin-bottom: 30px;
            line-height: 1.6;
        }
        
        .input-area {
            margin-bottom: 30px;
        }
        
        #loveInput {
            width: 100%;
            max-width: 400px;
            padding: 15px 20px;
            font-size: 1.2em;
            border: 3px solid #ffcccc;
            border-radius: 50px;
            outline: none;
            transition: all 0.3s ease;
            text-align: center;
        }
        
        #loveInput:focus {
            border-color: #ff4757;
            box-shadow: 0 0 15px rgba(255, 71, 87, 0.3);
        }
        
        #loveInput::placeholder {
            color: #aaa;
        }
        
        .heart-container {
            display: none;
            margin: 40px 0;
            animation: fadeIn 1s ease;
        }
        
        .heart {
            position: relative;
            width: 200px;
            height: 180px;
            margin: 0 auto 30px;
            animation: heartbeat 1.5s infinite ease-in-out;
        }
        
        .heart:before,
        .heart:after {
            content: "";
            position: absolute;
            left: 100px;
            top: 0;
            width: 100px;
            height: 160px;
            background: #ff4757;
            border-radius: 100px 100px 0 0;
            transform: rotate(-45deg);
            transform-origin: 0 100%;
        }
        
        .heart:after {
            left: 0;
            transform: rotate(45deg);
            transform-origin: 100% 100%;
        }
        
        .message {
            color: #ff4757;
            font-size: 2em;
            font-weight: bold;
            margin-top: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
        }
        
        .message span {
            display: inline-block;
            animation: float 3s ease-in-out infinite;
            animation-delay: calc(var(--i) * 0.1s);
        }
        
        .hint {
            color: #888;
            font-style: italic;
            margin-top: 20px;
            font-size: 1em;
        }
        
        .footer {
            margin-top: 40px;
            color: #999;
            font-size: 0.9em;
        }
        
        @keyframes heartbeat {
            0% { transform: scale(1); }
            5% { transform: scale(1.1); }
            10% { transform: scale(1); }
            15% { transform: scale(1.1); }
            20% { transform: scale(1); }
            100% { transform: scale(1); }
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
        
        .fireworks {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }
        
        @media (max-width: 600px) {
            .container {
                padding: 20px;
            }
            
            h1 {
                font-size: 2em;
            }
            
            .heart {
                width: 150px;
                height: 135px;
            }
            
            .heart:before,
            .heart:after {
                width: 75px;
                height: 120px;
                left: 75px;
            }
        }
    </style>
</head>
<body>
    <div class="fireworks" id="fireworks"></div>
    
    <div class="container">
        <h1>💖 Секретное Послание 💖</h1>
        
        <div class="instructions">
            <p>Введите волшебные слова в поле ниже, чтобы увидеть сюрприз!</p>
            <p><em>Подсказка: напишите "я люблю тебя"</em></p>
        </div>
        
        <div class="input-area">
            <input 
                type="text" 
                id="loveInput" 
                placeholder="Напишите волшебные слова здесь..." 
                autocomplete="off"
            >
        </div>
        
        <div class="heart-container" id="heartContainer">
            <div class="heart"></div>
            <div class="message" id="message">
                <!-- Текст будет добавлен через JavaScript -->
            </div>
            <div class="hint">💕 Сердечко бьется от счастья! 💕</div>
        </div>
        
        <div class="footer">
            <p>Создано с любовью ❤️</p>
            <p>Перезагрузите страницу, чтобы начать заново</p>
        </div>
    </div>

    <script>
        const loveInput = document.getElementById('loveInput');
        const heartContainer = document.getElementById('heartContainer');
        const message = document.getElementById('message');
        const fireworks = document.getElementById('fireworks');
        
        // Фраза, которую нужно ввести
        const magicPhrase = "я люблю тебя";
        
        loveInput.addEventListener('input', function() {
            const inputValue = this.value.toLowerCase().trim();
            
            if (inputValue === magicPhrase) {
                // Показываем сердечко
                heartContainer.style.display = 'block';
                
                // Создаем анимированный текст
                const loveText = "Я ЛЮБЛЮ ТЕБЯ!";
                message.innerHTML = '';
                
                for (let i = 0; i < loveText.length; i++) {
                    const span = document.createElement('span');
                    span.textContent = loveText[i];
                    span.style.setProperty('--i', i);
                    message.appendChild(span);
                }
                
                // Создаем фейерверки
                createFireworks();
                
                // Меняем фон
                document.body.style.background = 'linear-gradient(135deg, #ff6b6b 0%, #ff8e8e 100%)';
                
                // Очищаем поле ввода и меняем placeholder
                this.value = '';
                this.placeholder = 'Вы сделали это! ❤️';
                this.blur();
                
                // Добавляем звуковой эффект (опционально)
                playHeartSound();
                
                // Добавляем конфетти
                createConfetti();
            } else if (inputValue) {
                // Скрываем сердечко, если фраза не совпадает
                heartContainer.style.display = 'none';
            }
        });
        
        // Фокус на поле ввода при загрузке
        window.addEventListener('load', function() {
            loveInput.focus();
        });
        
        // Создание фейерверков
        function createFireworks() {
            fireworks.innerHTML = '';
            
            for (let i = 0; i < 50; i++) {
                const particle = document.createElement('div');
                particle.style.position = 'absolute';
                particle.style.width = '6px';
                particle.style.height = '6px';
                particle.style.backgroundColor = getRandomColor();
                particle.style.borderRadius = '50%';
                particle.style.left = Math.random() * 100 + 'vw';
                particle.style.top = Math.random() * 100 + 'vh';
                particle.style.boxShadow = '0 0 10px currentColor';
                
                // Анимация
                particle.animate([
                    { transform: 'translate(0, 0) scale(1)', opacity: 1 },
                    { transform: `translate(${Math.random() * 100 - 50}px, ${Math.random() * 100 - 50}px) scale(0)`, opacity: 0 }
                ], {
                    duration: 1000 + Math.random() * 1000,
                    delay: Math.random() * 1000
                });
                
                fireworks.appendChild(particle);
                
                // Удаляем частицу после анимации
                setTimeout(() => {
                    if (particle.parentNode === fireworks) {
                        fireworks.removeChild(particle);
                    }
                }, 2000);
            }
        }
        
        // Создание конфетти
        function createConfetti() {
            const confettiCount = 150;
            const confettiContainer = document.createElement('div');
            confettiContainer.style.position = 'fixed';
            confettiContainer.style.top = '0';
            confettiContainer.style.left = '0';
            confettiContainer.style.width = '100%';
            confettiContainer.style.height = '100%';
            confettiContainer.style.pointerEvents = 'none';
            confettiContainer.style.zIndex = '9999';
            
            document.body.appendChild(confettiContainer);
            
            for (let i = 0; i < confettiCount; i++) {
                const confetti = document.createElement('div');
                confetti.style.position = 'absolute';
                confetti.style.width = '10px';
                confetti.style.height = '10px';
                confetti.style.backgroundColor = getRandomColor();
                confetti.style.top = '-20px';
                confetti.style.left = Math.random() * 100 + 'vw';
                confetti.style.borderRadius = Math.random() > 0.5 ? '50%' : '0';
                confetti.style.opacity = '0.9';
                
                confettiContainer.appendChild(confetti);
                
                // Анимация падения
                const animation = confetti.animate([
                    { transform: 'translateY(0) rotate(0deg)', opacity: 1 },
                    { transform: `translateY(${window.innerHeight}px) rotate(${Math.random() * 360}deg)`, opacity: 0 }
                ], {
                    duration: 2000 + Math.random() * 2000,
                    easing: 'cubic-bezier(0.1, 0.8, 0.3, 1)',
                    delay: Math.random() * 1000
                });
                
                // Удаляем конфетти после анимации
                animation.onfinish = () => {
                    confetti.remove();
                    if (confettiContainer.children.length === 0) {
                        confettiContainer.remove();
                    }
                };
            }
        }
        
        function getRandomColor() {
            const colors = ['#ff4757', '#ff3838', '#ff9ff3', '#f368e0', '#ff9f43', '#ffaf40', '#32ff7e', '#7efff5', '#18dcff', '#7158e2'];
            return colors[Math.floor(Math.random() * colors.length)];
        }
        
        function playHeartSound() {
            // Простой звуковой эффект с использованием Web Audio API
            try {
                const audioContext = new (window.AudioContext || window.webkitAudioContext)();
                const oscillator = audioContext.createOscillator();
                const gainNode = audioContext.createGain();
                
                oscillator.connect(gainNode);
                gainNode.connect(audioContext.destination);
                
                oscillator.frequency.setValueAtTime(523.25, audioContext.currentTime); // Нота C5
                oscillator.frequency.setValueAtTime(659.25, audioContext.currentTime + 0.1); // Нота E5
                oscillator.frequency.setValueAtTime(783.99, audioContext.currentTime + 0.2); // Нота G5
                
                gainNode.gain.setValueAtTime(0.3, audioContext.currentTime);
                gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.5);
                
                oscillator.start(audioContext.currentTime);
                oscillator.stop(audioContext.currentTime + 0.5);
            } catch (e) {
                console.log("Аудио не поддерживается в этом браузере");
            }
        }
        
        // Добавляем эффект при наведении на поле ввода
        loveInput.addEventListener('mouseenter', function() {
            this.style.transform = 'scale(1.05)';
            this.style.transition = 'transform 0.3s ease';
        });
        
        loveInput.addEventListener('mouseleave', function() {
            this.style.transform = 'scale(1)';
        });
    </script>
</body>
</html>
